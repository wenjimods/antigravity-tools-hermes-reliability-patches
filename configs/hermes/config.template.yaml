# Merge only this section into the active profile. Do NOT replace the whole config.
# Use `hermes model` to configure the private key through the supported UI.
model:
  provider: custom
  base_url: http://127.0.0.1:8045/v1
  default: REPLACE_WITH_MODEL_FROM_AGT
